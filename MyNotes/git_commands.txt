After completing the code practice of each module, run these commands in right order:
git add .
git commit -m "Backend part-NUMBER completed"
git push
git push --set-upstream origin backend-part-NUMBER

And then go to the 'development' branch and run these commands:
git checkout development
git pull origin development
git checkout -b "backend-part-NewPartNUMBER"


